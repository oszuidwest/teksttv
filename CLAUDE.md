Playout renderer for cable TV text channels — displays slides and ticker from an external JSON playlist at a fixed resolution of 1920x1080.

Use `bun` as the package manager. Before committing, run `bun run check`. If it fails, run `bun run fix`.
