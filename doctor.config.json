{
  "$schema": "https://react.doctor/schema/config.json",
  "//": "dangerous-html-sink is suppressed only for CMS slide/ticker HTML documented in types.ts. URL overrides make the playout URL part of the trust boundary; the rule stays enabled elsewhere.",
  "ignore": {
    "overrides": [
      {
        "files": [
          "src/components/rucphen/TextSlide.tsx",
          "src/components/zuidwest/TextSlide.tsx",
          "src/components/zuidwest/Ticker.tsx"
        ],
        "rules": ["react-doctor/dangerous-html-sink"]
      }
    ]
  }
}
