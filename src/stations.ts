// CMS feed roots per broadcaster, shared by the station pages. ZuidWest's base
// is the complete channel-feed endpoint; Rucphen's is the namespace root for
// the split endpoints (see useCarousel for the mode semantics).
export const ZUIDWEST_API_BASE =
  'https://www.zuidwestupdate.nl/wp-json/zw/v1/teksttv'
export const RUCPHEN_API_BASE = 'https://cms.tv-krant.nl/wp-json/zw/v1'
